<Page x:Class="KsWare.Presentation.UITestApp.Pages.ActionTest"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:KsWare.Presentation.UITestApp.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="ActionTest">
    <Page.DataContext>
		<local:ActionTestVM/>
    </Page.DataContext>
    <StackPanel>
        <CheckBox Content="Throw Exception" IsChecked="{Binding ThrowException}"/>
	    <UniformGrid Rows="1">
			<Button Content="1" Command="{Binding Test1Action}"/>
			<Button Content="2" Command="{Binding Test2Action}"/>
			<Button Content="3" Command="{Binding Test3Action}"/>
			<Button Content="4" Command="{Binding Test4Action}"/>
	    </UniformGrid>
        <TextBox Text="{Binding Text}"/>
    </StackPanel>

</Page>
